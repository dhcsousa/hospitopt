apiVersion: v2
name: hospitopt
description: HospitOPT deployment (API, worker, frontend, and PostgreSQL)
type: application
version: 0.1.0
appVersion: "0.1.0"
dependencies:
  - name: postgresql
    version: "18.2.4"
    repository: "https://charts.bitnami.com/bitnami"
    condition: postgresql.enabled
